node_modules/
out/
.next/
# `next dev` builds here instead, so a dev server running in an editor cannot
# corrupt a production build's .next. See next.config.mjs.
.next-dev/
_pagefind/
*.log
.env
.env.*
!.env.local.example

# macOS / iCloud Drive metadata
.DS_Store
._*
*.icloud
.TemporaryItems/
.Trashes/
.fseventsd/
Icon?

context/
# Generated on demand (npm run backlog:status) — regenerable, do not commit
plan/status-report.md
# Personal Claude Code settings (permissions allowlist) — machine-local, not shared
.claude/settings.local.json
# Project-scoped MCP servers — machine-local, requires personal OAuth, not shared
.mcp.json
# Local experiments and artifacts
.firecrawl/
.agents/
.claude/
.gemini/
.opencode/
.codex/
.impeccable/
.playwright-mcp/
.vinext/
.open-next/
.wrangler/
cloudflare-env.d.ts
dist/
workers/
next-env.d.ts
CLAUDE.md
PRODUCT.md
app/generated/*
!app/generated/contributors.json
!app/generated/media.json
!app/generated/media-retired.json
# Derived content/SEO outputs; npm run manifest recreates them.
public/page-dates.json
public/page-published.json
public/page-verified.json
public/sitemap.xml
public/robots.txt
public/llms.txt
public/llms-full.txt
public/progress.json
public/progress.en.json
# Build-generated contributor social cards. The committed recognition snapshot
# is the source; every build replaces this directory and removes stale cards.
/public/contributor-cards/
# TypeScript incremental build cache
tsconfig.tsbuildinfo
*.tsbuildinfo

# Local editor/MCP configuration
opencode.json

# Media staging. Bytes live in R2, never in git — see AGENTS.md "Media".
/media/
